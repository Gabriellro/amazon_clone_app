import 'package:flutter/material.dart';

class BuyAgainTabPage extends StatelessWidget {
  const BuyAgainTabPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [Text('Comprar novamente')],
    );
  }
}
